from django.apps import AppConfig


class ServicesConfig(AppConfig):
    name = 'services'
    verbose_name='Gestor de servicios Laboratorio'

    #el verbose_ name solo se usa para traducir la vista
    # del admin en el panel admin del servidor#

